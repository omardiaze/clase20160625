<%= simple_form_for @categoty, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= error_span(@categoty[:name]) %>
  <%= f.input :description %>
  <%= error_span(@categoty[:description]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                categoties_path, :class => 'btn btn-default' %>
<% end %>
